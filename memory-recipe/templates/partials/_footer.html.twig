<footer>
<div class="divider"></div>
    <div>
        <p>Recettes de nos souvenirs</p>
        <div class="footer-links">
            {% set currentPath = app.request.attributes.get('_route') %}
            {# {{dump(currentPath)}} #}
            {# {% set currentPath = path(app.request.attributes.get('_route')) %}
            {% if currentPath != "/connexion" %} #}
            <a href="{{path('recipe_create')}}"{{ (currentPath == 'recipe_create') ? 'hidden' : ''}}>Ajouter ma recette</a>
            {# {% endif %} #}
            <a href="{{path('contact')}}"{{ (currentPath == 'contact') ? 'hidden' : ''}}>Contacts</a>
        </div>
    </div>
</footer>
    </body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <script src="{{asset('assets/js/app.js')}}"></script>
    <script src="{{asset('assets/js/component/ingredientForm.js')}}"></script>
</html>